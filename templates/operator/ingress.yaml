{{- if .Values.operator.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "trivy-operator-web-ui-operator.name" . }}
  labels:
    {{- include "trivy-operator-web-ui-operator.labels" . | nindent 4 }}
  {{- with .Values.operator.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.operator.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.operator.ingress.tls }}
  tls:
    {{- range .Values.operator.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.operator.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "trivy-operator-web-ui-operator.name" $ }}
                port:
                  number: {{ $.Values.operator.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
