apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "trivy-operator-web-ui-operator.name" . }}-env
  labels:
    {{- include "trivy-operator-web-ui-operator.labels" . | nindent 4 }}
data:
  RUST_LOG: {{ .Values.operator.log.level }}