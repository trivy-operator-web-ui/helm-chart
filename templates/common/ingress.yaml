{{- if .Values.common.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "trivy-operator-web-ui.name" . }}
  labels:
    {{- include "trivy-operator-web-ui.labels" . | nindent 4 }}
  {{- with .Values.common.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.common.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.common.ingress.tls }}
  tls:
    {{- range .Values.common.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.common.ingress.hosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "trivy-operator-web-ui-frontend.name" $ }}
                port:
                  number: {{ $.Values.frontend.service.port }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /api
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "trivy-operator-web-ui-operator.name" $ }}
                port:
                  number: {{ $.Values.operator.service.port }}
    {{- end }}
{{- end }}
